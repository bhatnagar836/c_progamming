#include<stdio.h>
int main(int argc, char const *argv[])
{
    int a, c, d, e, f;
    float b;
    a = 34;
    b = 6;
    c = 1;
    d = 0;
    e = 2;
    f = 3;
    // Arithmetic operators
    // printf("a+b = %f\n", a+b);
    // printf("a-b = %f\n", a-b);
    // printf("a*b = %f\n", a*b);
    // printf("a/b = %f\n", a/b);
    // printf("a%b = %f\n", a%b);

    // Logical operators
    // printf("d+c = %d\n", d||c);
    // printf("d-c = %d\n", d&&c);
    // printf("d*c = %d\n", !c);

    // Relational operators (<,>, ==, !=, <=, >=)

    //Bitwise operators
    printf("e|f = %d\n", e|f);
    printf("e&f = %d\n", e&f);
    printf("e^f = %d\n", e^f);
    // Binary left shift (<<), right shift(>>) and one's complement (~)

    // Assignment operators (=, +=, -=, *=, /=)

    // Miscellaneous operators (sizeof(), &, pointer(*), ?:)


    return 0;
}
